import { ReactElement } from 'react'
import styles from './Footer.module.scss'

function Footer(): ReactElement {
  return <footer className={styles.footer}>Anime App. 2023</footer>
}

export default Footer
